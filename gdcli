#!/usr/bin/env python3

# pass commdand line items into main cli-client
# check for basic sanity and requirements

import os
import sys
import json

try:
    import googleapiclient
    import google_auth_oauthlib
    import google.auth
except:
    print("Failed to import Google Drive API libraries.")
    raise

from gdclient import gdcli, cliclient

# cliclient.execute(os.getcwd(), sys.argv[1:])
gdcli.run()